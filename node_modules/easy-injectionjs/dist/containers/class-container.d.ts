import { IContainer } from '../core';
import { Container } from './container';
export declare class ClassContainer {
    private static _instance;
    private static _container;
    private static _parents;
    private constructor();
    static readonly Instance: ClassContainer;
    static readonly ContainerInstance: IContainer;
    static insertDependency(target: string, data: Container): void;
    static getDependency(target: string): Container;
    static getDepedencies(): IContainer;
    static readonly ParentsInstance: IContainer;
    static insertParent(target: string, data: Container): void;
    static getParent(target: string): Container;
    static getParents(): IContainer;
}
