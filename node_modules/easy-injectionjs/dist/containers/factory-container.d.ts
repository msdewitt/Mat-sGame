import { Container } from './';
import { Dependency, Stereotype } from '../core';
export declare class FactoryContainer extends Container {
    private _children;
    private _type;
    constructor(stereotype?: Stereotype, type?: Function, dependencies?: Dependency[], name?: string);
    readonly type: Function;
    addChild(child: Container): void;
    resolveDepedendencies(): Object;
    readonly instance: Object;
}
