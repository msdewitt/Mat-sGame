import { Stereotype, Dependency } from '../core';
export declare abstract class Container {
    protected _dependencies: Dependency[];
    protected _name: string;
    protected _stereotype: Stereotype;
    protected _instance: Object;
    protected _resolved: Dependency[];
    constructor(stereotype?: Stereotype, dependencies?: Dependency[], name?: string);
    stereotype: Stereotype;
    readonly abstract type: Object;
    instance: Object;
    dependencies: Dependency[];
    name: string;
    resolved: Dependency[];
    addDependency(v: Dependency): void;
    abstract resolveDepedendencies(): Object;
}
