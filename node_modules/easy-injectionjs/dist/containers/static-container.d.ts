import { Container } from './';
import { Stereotype, Dependency } from '../core';
export declare class StaticContainer extends Container {
    private _type;
    constructor(stereotype?: Stereotype, dependencies?: Dependency[], name?: string, type?: any);
    readonly type: any;
    resolveDepedendencies(): any;
}
