import { IContainer, Dependency } from './';
export declare class Wrapper {
    private static _instance;
    private static _container;
    private static _parents;
    private constructor();
    static readonly Instance: Wrapper;
    static readonly ContainerInstance: IContainer;
    static insertDependency(target: string, data: Dependency): void;
    static insertDepedencies(target: string, data: Dependency[]): void;
    static getDepedencies(target: string): Dependency[];
}
