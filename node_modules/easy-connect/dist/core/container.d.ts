import { Subscription } from 'rxjs/Rx';
export declare class Container {
    private _type;
    private _easy;
    _all: Subscription;
    _query: Subscription;
    _current: Subscription;
    private _id;
    constructor(_type?: {
        new (...args: any[]): {};
    });
    private createSubArray<T>(sub, obs, prefix, errCallBack?);
    private removeSub();
    private ensure();
    All(): Subscription;
    Current(selected: Object): Subscription;
    CurrentByKey(selected: Object, key: any): Subscription;
    Query<T extends {
        new (...args: any[]): {};
    }>(key: string, args: string): Subscription;
    add<T extends {
        new (...args: any[]): {};
    }>(data: T): void;
    update<T extends {
        new (...args: any[]): {};
    }>(data: T): void;
    delete<T extends {
        new (...args: any[]): {};
    }>(data: T): void;
}
