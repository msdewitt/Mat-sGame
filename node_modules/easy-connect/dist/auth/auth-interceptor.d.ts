import { AxiosInstance } from 'axios';
import { Observable } from 'rxjs/Rx';
export declare class HttpFactory {
    private _http;
    private _secureHttp;
    private cache;
    getHttp<T>(type: (new (...args: any[]) => T)): Observable<AxiosInstance>;
    private genHttp<T>(type);
}
