import { Token } from './';
import { Cache } from '../fetch';
import { Observable } from 'rxjs/Rx';
import { IAuth, IConfig } from '../core';
export declare class EasyTokenAuth implements IAuth {
    private config;
    private token;
    private cache;
    constructor(config?: IConfig, token?: Token, cache?: Cache);
    login(loginParams: Object): Observable<any>;
    logout(): Observable<any>;
    register(registerParams: Object): Observable<any>;
    validate(): Observable<any>;
    validateData(data: Object): Observable<any>;
}
export declare class EasyAuth implements IAuth {
    private config;
    constructor(config?: IConfig);
    login(loginParams: Object): Observable<any>;
    logout(): Observable<any>;
    validate(): Observable<any>;
    register(registerParams: Object): Observable<any>;
    validateData(data: Object): Observable<any>;
}
